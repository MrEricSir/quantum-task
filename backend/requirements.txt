fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.30
pydantic>=2.7.1
openai>=1.0.0
instructor>=1.0.0
requests>=2.31.0
icalendar>=5.0.0
recurring-ical-events>=3.0.0
pywebpush>=2.0.0
alembic>=1.13.0
# withings-api requires pydantic<2 but works fine at runtime with pydantic v2.
# Install with --no-deps to skip the version check; its non-pydantic deps below.
# withings-api>=2.4.0  ← installed separately in CI/setup (see dev.sh / deploy.yml)
arrow>=1.0.3
requests-oauthlib>=1.2
requests-oauth>=0.4.1
python-dotenv>=1.0.0
scipy>=1.13.0
